Re-enable drag-n-drop functionality
Add game over message
make it faster. changing the selected square should be instantaneous, nothing changes based on that

maybe add word validation stuff

make it pretty
